<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /
    
    # Se o arquivo/diretório não existir na raiz, redireciona para public
    RewriteCond %{DOCUMENT_ROOT}/public/$1 -f [OR]
    RewriteCond %{DOCUMENT_ROOT}/public/$1 -d
    RewriteRule ^(.*)$ public/$1 [L]
    
    # Se nada for encontrado, redireciona para o index.html em public
    RewriteRule ^$ public/index.html [L]
</IfModule>

# Definir index padrão
DirectoryIndex public/index.html

# Permitir acesso ao diretório
<Directory "/public_html/public">
    Options Indexes FollowSymLinks
    AllowOverride All
    Require all granted
</Directory>